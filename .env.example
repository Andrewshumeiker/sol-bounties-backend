# Railway sets PORT automatically
PORT=3001

# Comma-separated allowed origins (Vercel URL). Leave empty to allow all during dev.
CORS_ORIGIN=http://localhost:3000

# JWT
JWT_SECRET=change-me

# Solana RPC
SOLANA_RPC=https://api.devnet.solana.com
